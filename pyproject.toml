[project]
name = "trodestrack"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [{ name = "Eric Denovellis", email = "eric.denovellis@ucsf.edu" }]
requires-python = ">=3.11"
dependencies = []

[project.scripts]
trodestrack = "trodestrack:main"

[build-system]
requires = ["uv_build>=0.8.19,<0.9.0"]
build-backend = "uv_build"
